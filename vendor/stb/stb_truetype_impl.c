// stb_truetype implementation TU (public domain, v1.19 vendored from
// photon/src/lib/stb) — Zig 0.16 translate-c cannot compile the header, so the
// implementation is built as C and declared extern in src/renderer/gui_sdl.zig.
#define STB_TRUETYPE_IMPLEMENTATION 1
#include "stb_truetype.h"
