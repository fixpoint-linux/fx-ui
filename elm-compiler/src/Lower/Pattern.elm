module Lower.Pattern exposing (stub)

-- Placeholder for the M2 pattern compiler (M2 scope).  Kept so the module tree
-- compiles; the real pattern -> csexp lowering lands in the M2 sub-unit.
-- Lower/Expr.elm must not depend on this module until then.


stub : String
stub =
    "Lower.Pattern lowering lands in M2"
